# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

files
cors.json
.env
# direnv config — keep .envrc.example committed, ignore the real one
.envrc

# Firebase
.firebase/
# Service account credentials — NEVER commit these
*-firebase-adminsdk-*.json
service-account*.json
serviceAccount*.json
firebase-adminsdk.json

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/

# Temp seed files (used by manual testing only)
public/_seed.*

# Agent skills — regenerable from skills-lock.json (same pattern as
# node_modules + package-lock.json). The lockfile IS committed so
# everyone on the project resolves to the same skill versions.
.agents/skills/
